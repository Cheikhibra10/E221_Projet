services:
  - type: web
    name: pedagogie-service
    env: java
    plan: free
    rootDir: pedagogie-service
    buildCommand: ./mvnw clean install
    startCommand: java -Dserver.port=$PORT $JAVA_OPTS -jar target/pedagogie-service.jar
